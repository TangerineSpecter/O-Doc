# 【小橘文档】接口文档

本文档描述了 `小橘文档` 项目的API接口规范，包括接口列表、请求参数、响应格式等信息。

## 📋 文档说明

- 接口基础URL：`http://localhost:11800/api/v1`
- 请求方式：支持GET、POST、PUT、DELETE等HTTP方法
- 响应格式：统一返回JSON格式数据
- 认证方式：JWT Token认证（待实现）

[TOC]

## 🛠️ 接口分类

### 1. 系统设置接口

| 接口名称 | 请求方式 | 接口路径 | 功能描述 | 状态 |
|--------|------|----------------|----------|-----|
| 获取AI提供商列表 | GET | /api/settings/providers/ | 获取所有AI提供商配置 | 已实现 |
| 创建AI提供商 | POST | /api/settings/providers/ | 创建新的AI提供商配置 | 已实现 |
| 获取AI提供商详情 | GET | /api/settings/providers/:provider_id/ | 获取指定AI提供商详情 | 已实现 |
| 更新AI提供商 | PUT | /api/settings/providers/:provider_id/ | 更新指定AI提供商配置 | 已实现 |
| 删除AI提供商 | DELETE | /api/settings/providers/:provider_id/ | 删除指定AI提供商 | 已实现 |
| 获取AI模型列表 | GET | /api/settings/models/ | 获取所有AI模型配置 | 已实现 |
| 创建AI模型 | POST | /api/settings/models/ | 创建新的AI模型配置 | 已实现 |
| 获取AI模型详情 | GET | /api/settings/models/:model_id/ | 获取指定AI模型详情 | 已实现 |
| 更新AI模型 | PUT | /api/settings/models/:model_id/ | 更新指定AI模型配置 | 已实现 |
| 删除AI模型 | DELETE | /api/settings/models/:model_id/ | 删除指定AI模型 | 已实现 |
| 获取AI系统配置 | GET | /api/settings/config/get_ai_config/ | 获取系统AI配置 | 已实现 |
| 保存AI系统配置 | POST | /api/settings/config/save_ai_config/ | 保存系统AI配置 | 已实现 |

### 2. AI助手接口

| 接口名称 | 请求方式 | 接口路径 | 功能描述 | 状态 |
|--------|------|----------------|----------|-----|
| AI对话接口 | POST | /api/ai/chat/ | AI对话接口，支持流式响应 | 已实现 |

### 3. 用户管理接口

| 接口名称   | 请求方式 | 接口路径           | 功能描述     | 状态  |
|--------|------|----------------|----------|-----|
| 用户注册   | POST | /user/register | 用户注册新账号  | 待实现 |
| 用户登录   | POST | /user/login    | 用户登录系统   | 待实现 |
| 获取用户信息 | GET  | /user/info     | 获取当前用户信息 | 待实现 |
| 更新用户信息 | PUT  | /user/info     | 更新用户信息   | 待实现 |

### 2. 文章管理接口

**注意**：文章创建时，如果未指定 `author` 字段，系统将默认使用 `admin` 作为作者。

| 接口名称   | 请求方式   | 接口路径          | 功能描述   | 状态  |
|--------|--------|---------------|--------|-----|
| 创建文章   | POST   | /article/create | 创建新文章  | 已实现 |
| 获取文章列表 | GET    | /article/list | 获取文章列表 | 已实现 |
| 获取文章详情 | GET    | /article/detail/:article_id  | 获取文章详情 | 已实现 |
| 更新文章   | PUT    | /article/update/:article_id  | 更新文章内容 | 已实现 |
| 删除文章   | DELETE | /article/delete/:article_id  | 删除文章   | 已实现 |
| 获取文章树形列表 | GET | /article/tree-list | 获取树形结构文章列表 | 已实现 |

#### 2.1 创建文章接口

**请求路径**：`/api/article/create`
**请求方式**：`POST`
**请求参数**：

```json
{
  "title": "文章标题",
  "content": "文章内容（Markdown格式）",
  "collId": "所属文集ID",
  "permission": "public",
  "categoryId": "所属分类ID",
  "sort": 0
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| title | string | 是 | 文章标题 |
| content | string | 是 | 文章内容（Markdown格式） |
| collId | string | 是 | 所属文集ID |
| permission | string | 否 | 文章权限，可选值：public（公开）、private（私有），默认值：public |
| categoryId | string | 否 | 所属分类ID |
| sort | number | 否 | 文章排序值，值越小越靠前，默认值：0 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "articleId": "art_1234567890",
    "title": "文章标题",
    "content": "文章内容（Markdown格式）",
    "collId": "col_1234567890",
    "author": "admin",
    "createdAt": "2024-01-01T00:00:00Z",
    "updatedAt": "2024-01-01T00:00:00Z",
    "isValid": true,
    "permission": "public",
    "readCount": 0,
    "categoryId": "cat_1234567890",
    "sort": 0
  }
}
```

#### 2.2 获取文章列表接口

**请求路径**：`/api/article/list`
**请求方式**：`GET`
**请求参数**：

| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| collId | string | 否 | 文集ID |
| tagId | string | 否 | 标签ID |
| categoryId | string | 否 | 分类ID |
| keyword | string | 否 | 关键词（标题模糊检索） |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "articleId": "art_1234567890",
      "title": "文章标题",
      "content": "文章内容（Markdown格式）",
      "collId": "col_1234567890",
      "author": "admin",
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-01T00:00:00Z",
      "isValid": true,
      "permission": "public",
      "readCount": 0,
      "categoryId": "cat_1234567890",
      "sort": 0
    }
  ]
}
```

#### 2.3 获取文章详情接口

**请求路径**：`/api/article/detail/:article_id`
**请求方式**：`GET`
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "articleId": "art_1234567890",
    "title": "文章标题",
    "content": "文章内容（Markdown格式）",
    "collId": "col_1234567890",
    "author": "admin",
    "createdAt": "2024-01-01T00:00:00Z",
    "updatedAt": "2024-01-01T00:00:00Z",
    "isValid": true,
    "permission": "public",
    "readCount": 1,
    "categoryId": "cat_1234567890",
    "sort": 0
  }
}
```

#### 2.4 更新文章接口

**请求路径**：`/api/article/update/:article_id`
**请求方式**：`PUT`
**请求参数**：

```json
{
  "title": "更新后的文章标题",
  "content": "更新后的文章内容（Markdown格式）",
  "permission": "private",
  "categoryId": "新的分类ID",
  "sort": 1
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| title | string | 否 | 文章标题 |
| content | string | 否 | 文章内容（Markdown格式） |
| permission | string | 否 | 文章权限，可选值：public（公开）、private（私有） |
| categoryId | string | 否 | 所属分类ID |
| sort | number | 否 | 文章排序值，值越小越靠前 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "articleId": "art_1234567890",
    "title": "更新后的文章标题",
    "content": "更新后的文章内容（Markdown格式）",
    "collId": "col_1234567890",
    "author": "admin",
    "createdAt": "2024-01-01T00:00:00Z",
    "updatedAt": "2024-01-02T00:00:00Z",
    "isValid": true,
    "permission": "private",
    "readCount": 1,
    "categoryId": "cat_0987654321",
    "sort": 1
  }
}
```

#### 2.5 删除文章接口

**请求路径**：`/api/article/delete/:article_id`
**请求方式**：`DELETE`
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 3. 分类管理接口

| 接口名称      | 请求方式   | 接口路径                                | 功能描述       | 状态  |
|-----------|--------|-------------------------------------|------------|-----|
| 创建分类      | POST   | /api/category/create                | 创建新分类      | 已实现 |
| 获取分类列表    | GET    | /api/category/list                  | 获取分类列表     | 已实现 |
| 获取分类详情    | GET    | /api/category/detail/:category_id   | 获取分类详情     | 已实现 |
| 更新分类      | PUT    | /api/category/update/:category_id   | 更新分类信息     | 已实现 |
| 删除分类      | DELETE | /api/category/delete/:category_id   | 删除分类       | 已实现 |
| 分类排序      | PUT    | /api/category/:category_id/sort     | 更新分类排序     | 已实现 |


#### 3.1 创建分类接口

**请求路径**：`/api/category/create`
**请求方式**：POST
**请求参数**：

```json
{
  "name": "分类名称",
  "description": "分类描述"
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 是 | 分类名称，最长10个字符 |
| description | string | 否 | 分类描述，最长100个字符 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "category_id": "cat_1234567890",
    "name": "分类名称",
    "description": "分类描述",
    "userid": "admin",
    "is_valid": true,
    "sort": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-01T00:00:00Z"
  }
}
```

#### 3.2 获取分类列表接口

**请求路径**：`/api/category/list`
**请求方式**：GET
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "category_id": "cat_1234567890",
      "name": "技术文档",
      "description": "技术相关文档",
      "userid": "admin",
      "is_valid": true,
      "sort": 0,
      "created_at": "2025-01-01T00:00:00Z",
      "updated_at": "2025-01-01T00:00:00Z"
    },
    {
      "category_id": "cat_0987654321",
      "name": "产品需求",
      "description": "产品需求文档",
      "userid": "admin",
      "is_valid": true,
      "sort": 1,
      "created_at": "2025-01-02T00:00:00Z",
      "updated_at": "2025-01-02T00:00:00Z"
    }
  ]
}
```

**字段说明**：
| 字段名 | 类型 | 描述 |
|-------|------|------|
| category_id | string | 分类唯一标识 |
| name | string | 分类名称 |
| description | string | 分类描述 |
| userid | string | 创建者ID |
| is_valid | boolean | 是否有效 |
| sort | integer | 排序序号 |
| created_at | string | 创建时间 |
| updated_at | string | 更新时间 |

#### 3.3 获取分类详情接口

**请求路径**：`/api/category/detail/:category_id`
**请求方式**：GET
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "category_id": "cat_1234567890",
    "name": "技术文档",
    "description": "技术相关文档",
    "userid": "admin",
    "is_valid": true,
    "sort": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-01T00:00:00Z"
  }
}
```

#### 3.6 分类排序接口

**请求路径**：`/api/category/:category_id/sort`
**请求方式**：PUT
**请求参数**：

```json
{
  "sort": 2
}
```

**请求参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| sort | integer | 是 | 排序位置（0表示第一位，1表示第二位，以此类推） |

**响应示例**：

```json
{
  "code": 200,
  "message": "排序成功",
  "data": null
}
```

#### 3.4 更新分类接口

**请求路径**：`/api/category/update/:category_id`
**请求方式**：PUT
**请求参数**：

```json
{
  "name": "更新后的分类名称",
  "description": "更新后的分类描述"
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 否 | 分类名称，最长10个字符 |
| description | string | 否 | 分类描述，最长100个字符 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "category_id": "cat_1234567890",
    "name": "更新后的分类名称",
    "description": "更新后的分类描述",
    "userid": "admin",
    "is_valid": true,
    "sort": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-03T00:00:00Z"
  }
}
```

#### 3.5 删除分类接口

**请求路径**：`/api/category/delete/:category_id`
**请求方式**：DELETE
**响应示例**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

**注意**：

- 该接口执行的是逻辑删除，将分类的is_valid字段设置为false
- 删除后，该分类将不再出现在列表接口中

### 4. 标签管理接口

| 接口名称    | 请求方式   | 接口路径                    | 功能描述            | 状态  |
|---------|--------|-------------------------|-----------------|-----|
| 创建标签    | POST   | /api/tag/create         | 创建新标签           | 已实现 |
| 获取标签详情  | GET    | /api/tag/detail/:tag_id | 获取标签详情          | 已实现 |
| 获取标签列表  | GET    | /api/tag/list           | 获取标签列表          | 已实现 |
| 标签排序    | PUT    | /api/tag/:tag_id/sort   | 更新标签排序          | 已实现 |
| 更新标签    | PUT    | /api/tag/update/:tag_id | 更新标签信息          | 已实现 |
| 删除标签    | DELETE | /api/tag/delete/:tag_id | 删除标签            | 已实现 |


#### 4.1 创建标签接口

**请求路径**：`/api/tag/create`
**请求方式**：POST
**请求参数**：

```json
{
  "name": "标签名称",
  "themeId": "主题ID"
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 是 | 标签名称，最长30个字符 |
| themeId | string | 是 | 主题ID，用于标签的颜色主题 |

**响应示例**：

```json
{
  "code": 200,
  "message": "标签创建成功",
  "data": {
    "tag_id": "tag_001",
    "name": "标签名称",
    "themeId": "theme_001",
    "sort": 1,
    "created_at": "2025-01-16T10:30:00Z",
    "updated_at": "2025-01-16T10:30:00Z"
  }
}
```

#### 4.2 获取标签详情接口

**请求路径**：`/api/tag/detail/:tag_id`
**请求方式**：GET
**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| tag_id | string | 是 | 标签ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "tag_id": "tag_001",
    "name": "标签名称",
    "themeId": "theme_001",
    "sort": 1,
    "created_at": "2025-01-16T10:30:00Z",
    "updated_at": "2025-01-16T10:30:00Z"
  }
}
```

#### 4.3 获取标签列表接口

**请求路径**：`/api/tag/list`
**请求方式**：GET

**响应示例**：

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "tag_id": "tag_001",
      "name": "标签1",
      "themeId": "theme_001",
      "sort": 1,
      "created_at": "2025-01-16T10:30:00Z",
      "updated_at": "2025-01-16T10:30:00Z"
    },
    {
      "tag_id": "tag_002",
      "name": "标签2",
      "themeId": "theme_002",
      "sort": 2,
      "created_at": "2025-01-16T10:35:00Z",
      "updated_at": "2025-01-16T10:35:00Z"
    }
  ]
}
```

#### 4.4 标签排序接口

**请求路径**：`/api/tag/:tag_id/sort`
**请求方式**：PUT
**请求参数**：

```json
{
  "sort": 3
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| sort | integer | 是 | 排序值，数值越大，排序越靠前 |

**响应示例**：

```json
{
  "code": 200,
  "message": "排序成功",
  "data": null
}
```

#### 4.5 更新标签接口

**请求路径**：`/api/tag/update/:tag_id`
**请求方式**：PUT
**请求参数**：

```json
{
  "name": "更新后的标签名称",
  "themeId": "更新后的主题ID"
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 否 | 标签名称，最长30个字符 |
| themeId | string | 否 | 主题ID，用于标签的颜色主题 |

**响应示例**：

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "tag_id": "tag_001",
    "name": "更新后的标签名称",
    "themeId": "更新后的主题ID",
    "sort": 1,
    "created_at": "2025-01-16T10:30:00Z",
    "updated_at": "2025-01-16T11:00:00Z"
  }
}
```

#### 4.6 删除标签接口

**请求路径**：`/api/tag/delete/:tag_id`
**请求方式**：DELETE
**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| tag_id | string | 是 | 标签ID |

**响应示例**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 5. 文集管理接口

| 接口名称   | 请求方式   | 接口路径                           | 功能描述            | 状态  |
|--------|--------|--------------------------------|-----------------|-----|
| 创建文集   | POST   | /api/anthology/create          | 创建新文集           | 已实现 |
| 获取文集列表 | GET    | /api/anthology/list            | 获取文集列表          | 已实现 |
| 获取文集详情 | GET    | /api/anthology/detail/:coll_id | 根据coll_id获取文集详情 | 已实现 |
| 更新文集   | PUT    | /api/anthology/update/:coll_id | 更新文集信息          | 已实现 |
| 删除文集   | DELETE | /api/anthology/delete/:coll_id | 删除文集            | 已实现 |

#### 5.1 创建文集接口

**请求路径**：`/api/anthology/create`
**请求方式**：POST
**请求参数**：

```json
{
  "title": "文集标题",
  "description": "文集描述",
  "icon_id": "book",
  "permission": "public"
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| title | string | 是 | 文集标题，最长20个字符 |
| description | string | 否 | 文集描述，最长100个字符 |
| icon_id | string | 是 | 图标ID，用于前端显示图标 |
| permission | string | 是 | 访问权限，可选值：public（公开）、private（私密） |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "coll_id": "coll_1234567890",
    "title": "文集标题",
    "description": "文集描述",
    "icon_id": "book",
    "permission": "public",
    "is_top": false,
    "count": 0,
    "sort": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-01T00:00:00Z"
  }
}
```

#### 5.2 获取文集列表接口

**请求路径**：`/api/anthology/list`
**请求方式**：GET
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "coll_id": "coll_1234567890",
      "title": "小橘部署指南",
      "count": 42,
      "icon_id": "cpu",
      "isTop": true,
      "description": "全面介绍小橘文档私有化部署方案",
      "articles": [
        {
          "article_id": "art_001",
          "title": "服务器环境依赖检查清单",
          "date": "11-24"
        },
        {
          "article_id": "art_002",
          "title": "Docker Compose 一键部署",
          "date": "11-20"
        }
      ],
      "permission": "public"
    },
    {
      "id": 2,
      "coll_id": "coll_0987654321",
      "title": "API 接口开发手册",
      "count": 128,
      "icon_id": "zap",
      "isTop": true,
      "description": "后端接口定义、鉴权机制及错误码字典查询",
      "articles": [
        {
          "article_id": "art_003",
          "title": "认证鉴权：Access Token 获取",
          "date": "12-01"
        }
      ],
      "permission": "public"
    }
  ]
}
```

**字段说明**：
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | number | 文集数据库ID |
| coll_id | string | 文集唯一标识 |
| title | string | 文集标题 |
| count | number | 文章数量 |
| icon_id | string | 图标ID |
| isTop | boolean | 是否置顶 |
| description | string | 文集描述 |
| articles | array | 文章摘要列表，最多返回前3篇文章 |
| permission | string | 访问权限 |

**articles数组字段说明**：
| 字段名 | 类型 | 描述 |
|-------|------|------|
| article_id | string | 文章唯一标识 |
| title | string | 文章标题 |
| date | string | 更新日期，格式：MM-DD |

#### 5.3 获取文集详情接口

**请求路径**：`/api/anthology/detail/:coll_id`
**请求方式**：GET
**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "coll_id": "coll_1234567890",
    "title": "小橘部署指南",
    "description": "全面介绍小橘文档私有化部署方案",
    "icon_id": "cpu",
    "userid": "admin",
    "permission": "public",
    "is_top": true,
    "count": 42,
    "sort": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-01T00:00:00Z"
  }
}
```

#### 5.4 文集排序接口

**请求路径**：`/api/anthology/:coll_id/sort`
**请求方式**：PUT
**请求参数**：

```json
{
  "sort": 2
}
```

**请求参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| sort | integer | 是 | 排序位置（大于0的整数，1表示第一位，2表示第二位，仅对非置顶文集有效） |

**响应示例**：

```json
{
  "code": 200,
  "message": "排序成功",
  "data": null
}
```

**错误响应示例**：

```json
{
  "code": 400,
  "message": "置顶文集不允许排序",
  "data": null
}
```

**注意**：

- 仅对非置顶文集进行排序
- 排序位置必须是大于0的整数

#### 5.4 更新文集接口

**请求路径**：`/api/anthology/update/:coll_id`
**请求方式**：PUT
**请求参数**：

```json
{
  "title": "更新后的文集标题",
  "description": "更新后的文集描述",
  "icon_id": "update-icon",
  "permission": "private",
  "is_top": true
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| title | string | 否 | 文集标题，最长20个字符 |
| description | string | 否 | 文集描述，最长100个字符 |
| icon_id | string | 否 | 图标ID，用于前端显示图标 |
| permission | string | 否 | 访问权限，可选值：public（公开）、private（私密） |
| is_top | boolean | 否 | 是否置顶 |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "coll_id": "coll_1234567890",
    "title": "更新后的文集标题",
    "description": "更新后的文集描述",
    "icon_id": "update-icon",
    "userid": "admin",
    "permission": "private",
    "is_top": true,
    "count": 0,
    "created_at": "2025-01-01T00:00:00Z",
    "updated_at": "2025-01-02T00:00:00Z"
  }
}
```

#### 5.5 删除文集接口

**请求路径**：`/api/anthology/delete/:coll_id`
**请求方式**：DELETE
**响应示例**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

**注意**：

- 该接口执行的是逻辑删除，将文集的is_valid字段设置为false
- 删除后，该文集将不再出现在列表接口中

### 6. 资源管理接口

| 接口名称   | 请求方式   | 接口路径                        | 功能描述   | 状态  |
|--------|--------|-----------------------------|--------|-----|
| 获取资源列表 | GET    | /api/resource/list         | 获取资源列表 | 已实现 |
| 资源上传   | POST   | /api/resource/upload       | 上传资源文件  | 已实现 |
| 创建资源   | POST   | /api/resource/create       | 创建新资源记录  | 已实现 |
| 更新资源   | PUT    | /api/resource/update/:resource_id          | 更新资源信息 | 已实现 |
| 删除资源   | DELETE | /api/resource/delete/:resource_id          | 删除资源   | 已实现 |
| 下载资源   | GET    | /api/resource/download/:resource_id | 下载资源   | 已实现 |
| 查看资源   | GET    | /api/resource/view/:resource_id | 查看资源（图片预览）   | 已实现 |

#### 6.1 获取资源列表接口

**请求路径**：`/api/resource/list`
**请求方式**：GET
**请求参数**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| page | integer | 否 | 页码，默认1 |
| pageSize | integer | 否 | 每页数量，默认24 |
| type | string | 否 | 资源类型（doc/image/video/audio/code/archive/design） |
| linked | boolean | 否 | 是否已关联（true/false） |
| keyword | string | 否 | 搜索关键词 |
| sourceType | string | 否 | 资源来源类型（attachment/附件, content/内容, other/其他） |

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "data": [
      {
        "id": "res_1234567890",
        "name": "需求说明书_v1.pdf",
        "type": "doc",
        "size": "2.5 MB",
        "date": "11-24",
        "linked": true,
        "sourceType": "attachment",
        "sourceArticle": {
          "id": "art_001",
          "title": "服务器环境依赖检查清单"
        }
      }
    ],
    "total": 100
  }
}
```

#### 6.2 资源上传接口

**请求路径**：`/api/resource/upload`
**请求方式**：`POST`
**请求参数**：
- `file`: 要上传的文件（multipart/form-data格式）
- `linked`: 是否关联文章（可选，布尔值）
- `sourceArticle`: 关联的文章ID（可选，字符串）
- `source_type`: 资源来源类型（可选，字符串），取值范围：
  - `attachment`: 附件上传
  - `content`: 笔记编辑器内插入上传
  - `other`: 其他来源（默认）

**说明**：
- 当从笔记编辑器内插入上传图片等资源时，请设置`source_type=content`
- 当从附件管理界面上传资源时，请设置`source_type=attachment`

**响应示例**：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "resource_id": "res_1234567890",
    "name": "需求说明书_v1.pdf",
    "type": "doc",
    "size": "2.5 MB",
    "date": "11-24",
    "linked": true,
    "sourceType": "attachment",
    "sourceArticle": {
      "id": "art_001",
      "title": "服务器环境依赖检查清单"
    }
  }
}
```

#### 6.4 创建资源接口

**请求路径**：`/api/resource/create`
**请求方式**：`POST`
**请求参数**：

```json
{
  "name": "需求说明书_v1.pdf",
  "type": "doc",
  "size": "2.5 MB",
  "date": "11-24",
  "linked": false,
  "sourceType": "attachment",
  "sourceArticle": null
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 是 | 资源名称 |
| type | string | 是 | 资源类型 |
| size | string | 是 | 资源大小 |
| date | string | 是 | 创建日期 |
| linked | boolean | 是 | 是否已关联 |
| sourceType | string | 否 | 资源来源类型（attachment/附件, content/内容, other/其他） |
| sourceArticle | object | 否 | 关联的文章信息 |

#### 6.5 更新资源接口

**请求路径**：`/api/resource/update/:resource_id`
**请求方式**：PUT
**请求参数**：

```json
{
  "name": "需求说明书_v2.pdf",
  "linked": true,
  "sourceType": "attachment",
  "sourceArticle": {
    "id": "art_001",
    "title": "服务器环境依赖检查清单"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 描述 |
|-------|------|------|------|
| name | string | 否 | 资源名称 |
| type | string | 否 | 资源类型 |
| size | string | 否 | 资源大小 |
| date | string | 否 | 创建日期 |
| linked | boolean | 否 | 是否已关联 |
| sourceType | string | 否 | 资源来源类型（attachment/附件, content/内容, other/其他） |
| sourceArticle | object | 否 | 关联的文章信息 |

#### 6.5 删除资源接口

**请求路径**：`/api/resource/delete/:resource_id`
**请求方式**：`DELETE`
**响应示例**：

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

#### 6.6 下载资源接口

**请求路径**：`/api/resource/download/:resource_id`
**请求方式**：`GET`
**响应**：返回文件流

#### 6.7 查看资源接口

**请求路径**：`/api/resource/view/:resource_id`
**请求方式**：`GET`
**响应**：返回文件流（用于浏览器直接显示，如图片预览）

### 7. 统计接口

| 接口名称   | 请求方式 | 接口路径           | 功能描述     | 状态  |
|--------|------|----------------|----------|-----|
| 获取系统统计 | GET  | /api/stats/system  | 获取系统统计数据 | 待实现 |
| 获取文章统计 | GET  | /api/stats/article | 获取文章统计数据 | 待实现 |

## 📊 响应格式

### 成功响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    // 具体接口返回数据
  }
}
```

### 错误响应格式

```json
{
  "code": 错误码,
  "message": "错误信息",
  "data": null
}
```

## 📝 错误码说明

| 错误码 | 错误信息                  | 说明      |
|-----|-----------------------|---------|
| 200 | success               | 请求成功    |
| 400 | Bad Request           | 请求参数错误  |
| 401 | Unauthorized          | 未授权访问   |
| 403 | Forbidden             | 禁止访问    |
| 404 | Not Found             | 请求资源不存在 |
| 500 | Internal Server Error | 服务器内部错误 |
| 501 | Not Implemented       | 接口未实现   |

## 🚀 认证方式

### JWT Token认证（待实现）

1. 用户登录成功后获取Token
2. 在请求头中添加Authorization字段：`Authorization: Bearer {token}`
3. Token有效期：7天

## 📌 开发计划

- 暂无

## 📅 更新日志

- 暂无
