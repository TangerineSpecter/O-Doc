export interface LocationPoint {
  country: string;
  city: string;
  latitude: number;
  longitude: number;
  count: number;
}

export interface FocalLengthStat {
  name: string;
  count: number;
  numericValue: number;
}
