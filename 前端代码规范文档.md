# 前端代码规范文档

本文档用于约束 `frontend_react` 目录下的 React + TypeScript 代码风格。内容已根据当前项目实现校对，重点保证和现有代码一致，而不是照搬通用模板。

## 1. 命名规范

### 1.1 变量与函数

- 统一使用驼峰命名法 `camelCase`
- 变量名要表达语义，避免无意义缩写
- 布尔变量优先使用 `is`、`has`、`should`、`can` 等前缀

```ts
const isLoading = true;
const searchQuery = '';
const defaultEmbeddingModelId = '';
const handleSubmit = () => {};
```

### 1.2 组件名与文件名

- React 组件使用大驼峰命名法 `PascalCase`
- 组件文件名与组件名保持一致
- Hook 文件使用 `useXxx.ts` 或 `useXxx.tsx`

```tsx
const SearchModal = () => {};
const AISettings = () => {};
```

### 1.3 类型名

- TypeScript 类型、接口使用大驼峰命名法 `PascalCase`
- 当前项目里已经同时存在 `interface` 和 `type`
- 不强制要求使用 `IUser` / `TUser` 这种前缀，关键是同一文件内部保持一致

```ts
interface ApiResponse<T> {
  code: number;
  msg: string;
  data: T;
}

type ResourceType = 'doc' | 'image' | 'video';
```

## 2. 接口字段规范

### 2.1 前端只使用驼峰字段

- 前端请求参数统一使用驼峰命名
- 前端响应数据统一按驼峰字段读取
- 不要在前端手动混用 `snake_case` 和 `camelCase`

```ts
const params = {
  pageSize: 20,
  searchQuery: 'python',
  linkedArticleId: 'art_001',
};

const isTop = data.isTop;
const sourceArticle = data.sourceArticle;
const defaultEmbeddingModelId = config.defaultEmbeddingModelId;
```

### 2.2 不手写字段转换逻辑

- 当前项目后端已启用驼峰转换，前端不需要自己写一层 `user_name -> userName` 的映射
- 新增接口时，优先保持前端类型定义就是最终消费形态

## 3. 组件与页面组织

### 3.1 组件职责

- 页面级组件放在 `src/views`
- 通用组件放在 `src/components`
- 布局组件放在 `src/layout`
- 公共逻辑优先抽到 `src/hooks`

### 3.2 复杂状态优先抽 Hook

- 当页面同时包含数据加载、筛选、分页、交互状态时，优先抽成自定义 Hook
- 页面组件主要负责拼装 UI，不要把所有请求和状态都堆在一个文件里

## 4. 用户提示与交互

### 4.1 使用 toast，不直接用 alert

- 当前项目已有 `ToastProvider`
- 用户反馈优先使用 `toast.success`、`toast.error`、`toast.info`

```tsx
toast.success('保存成功');
toast.error('删除失败，请重试');
```

### 4.2 异步操作要有清晰状态

- 请求开始时设置加载态
- 请求结束时关闭加载态
- 失败时给出明确提示，不要静默失败

## 5. API 调用规范

### 5.1 统一从 `src/api` 发请求

- 接口请求按业务领域拆分到 `src/api/*`
- 页面和组件尽量不要直接在内部拼接 axios 请求

### 5.2 统一响应格式

- 当前项目前端按 `{ code, msg, data }` 结构消费后端响应
- 成功时从 `data` 里读取业务结果
- 错误提示优先使用 `msg`

对应实现参考：

- [frontend_react/src/utils/request.ts](/Users/zhouliangjun/Desktop/web_code/O-Doc/frontend_react/src/utils/request.ts)

## 6. 样式规范

### 6.1 优先使用现有 Tailwind 风格

- 继续沿用项目已有的 Tailwind 原子类风格
- 相同语义的颜色、圆角、阴影尽量复用已有写法
- 不要在同一页面混入完全不同的视觉体系

### 6.2 类名以可读性优先

- 类名过长时，可以按布局、颜色、交互分组换行
- 不为“少几个字符”牺牲可维护性

## 7. 当前项目确认过的事实

- 前端字段统一用驼峰命名，例如 `isTop`、`searchQuery`、`pageSize`、`sourceArticle`
- 前端请求基于 `/api`
- 后端返回字段中的 `snake_case` 会自动转换成前端使用的 `camelCase`

如果新增接口出现字段风格不一致，优先修正后端转换链路，不建议在前端单独做特例兼容。
