# 后端代码规范文档

本文档用于约束 Django / DRF / Python 侧代码风格。内容已结合当前项目配置校对，重点说明本项目已经生效的字段转换和响应约定。

## 1. 命名规范

### 1.1 变量、函数、模块

- 统一使用下划线命名法 `snake_case`
- 函数名、局部变量名、模块名都保持这一规则
- 变量要有业务语义，不使用难理解的缩写

```python
is_loading = True
user_name = "admin"
search_query = request.GET.get("search_query")

def create_user():
    pass
```

### 1.2 类名

- 类名使用大驼峰命名法 `PascalCase`
- Django 模型、序列化器、视图类都遵循这一规则

```python
class UserProfile(models.Model):
    pass

class ResourceUploadView(APIView):
    pass
```

### 1.3 模型字段与序列化器字段

- 模型字段统一使用下划线命名法
- 序列化器中的字段名默认与模型字段保持一致
- 不要为了迎合前端在后端内部改成驼峰字段

```python
class User(models.Model):
    user_name = models.CharField(max_length=100)
    create_time = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

## 2. 前后端字段转换规范

### 2.1 当前项目已启用自动转换

后端当前已经启用以下配置：

- `CamelCaseJSONParser`
- `CamelCaseJSONRenderer`
- `CamelCaseMiddleWare`

这意味着：

- 前端传入 `camelCase` 请求字段，后端会自动解析为 `snake_case`
- 后端返回 `snake_case` 字段，响应会自动渲染为 `camelCase`

对应实现参考：

- [o_doc/settings.py](/Users/zhouliangjun/Desktop/web_code/O-Doc/o_doc/settings.py)

### 2.2 后端内部仍坚持 snake_case

- Python 代码里只使用 `snake_case`
- 不要在 view / serializer / model 里手动改成 `pageSize`、`searchQuery`、`isTop`
- 这些转换由全局配置负责，不要在业务代码里重复处理

## 3. 参数校验规范

### 3.1 优先用序列化器校验

- 请求参数优先交给 DRF `serializer` 校验
- 校验规则尽量写在序列化器里
- 视图层负责调用和分支处理，不堆过多字段校验细节

### 3.2 简单查询参数可直接读取

- 对于列表筛选、分页这类简单 GET 参数，可以直接从 `request.GET` 读取
- 但命名仍保持后端内部的 `snake_case`

```python
search_query = request.GET.get('search_query')
page_size = int(request.GET.get('page_size', 20))
```

## 4. 响应规范

### 4.1 统一使用响应工具函数

- 当前项目统一通过 `success_result`、`valid_result`、`error_result`、`list_result` 返回数据
- 尽量不要在业务视图里自己拼裸 `Response({...})`

对应实现参考：

- [utils/response_utils.py](/Users/zhouliangjun/Desktop/web_code/O-Doc/utils/response_utils.py)

### 4.2 当前统一响应字段

当前项目响应结构是：

```json
{
  "code": 200,
  "msg": "成功",
  "data": {}
}
```

注意这里实际字段是 `msg`，不是 `message`。新增接口和文档都要跟当前实现保持一致。

## 5. 错误码规范

### 5.1 统一使用 `ErrorCode`

- 错误码统一定义在 `utils.error_codes.ErrorCode`
- 业务代码里不要直接写魔法数字

```python
return error_result(ErrorCode.RESOURCE_NOT_FOUND)
```

## 6. 视图与业务逻辑分层

### 6.1 视图负责入口与调度

- 视图主要负责：
  - 读取请求
  - 调用校验
  - 调用模型或服务逻辑
  - 返回统一响应

### 6.2 复杂逻辑尽量下沉

- 通用逻辑、集成逻辑、第三方服务逻辑优先放到 `utils/` 或对应模块中
- 不要把所有业务细节都堆进 `views.py`

## 7. 数据与时间字段

### 7.1 时间格式遵循全局设置

- 当前 DRF 全局时间格式为 `%Y-%m-%d %H:%M:%S`
- 如果要修改时间返回格式，应优先修改全局配置，而不是在单个接口里临时各写一套

## 8. 当前项目确认过的事实

- 后端内部命名统一应为 `snake_case`
- 前端看到的接口字段统一为 `camelCase`
- 响应统一字段是 `code`、`msg`、`data`
- 驼峰转换由全局配置负责，不建议在单个接口里手动重复转换

如果未来要改变这一套约定，应优先整体调整配置和文档，而不是在局部接口中做例外处理。
