# 代码规范文档

项目的代码规范已拆分为前后端两份独立文档，便于分别维护：

- [前端代码规范文档](前端代码规范文档.md)
- [后端代码规范文档](后端代码规范文档.md)

当前项目的命名和接口转换约定如下：

- 前端代码、前端类型、前端接口字段统一使用驼峰命名法 `camelCase`
- 后端 Python 代码、模型字段、序列化器字段统一使用下划线命名法 `snake_case`
- 前后端通过全局配置自动完成字段名转换：
  - 前端发送驼峰参数，后端会自动解析为下划线字段
  - 后端返回下划线字段，响应会自动渲染为驼峰字段
