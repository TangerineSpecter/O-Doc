# 代码规范文档

## 1. 前端代码规范

### 1.1 命名规范

#### 1.1.1 变量和函数命名
- 使用 **驼峰命名法** (camelCase)
- 变量名应具有描述性，避免使用缩写
- 布尔值变量名通常以 `is`、`has`、`should` 开头

```typescript
// 好的命名
const isLoading = true;
const hasPermission = false;
const shouldUpdate = true;
const userName = '张三';
const getUserInfo = () => {};

// 不好的命名
const a = true;
const flag = false;
const uName = '张三';
const getInfo = () => {};
```

#### 1.1.2 组件命名
- 使用 **大驼峰命名法** (PascalCase)
- 组件名应清晰描述其功能
- 文件名将组件名保持一致

```typescript
// 组件名
const UserProfile: React.FC = () => {};

// 文件名: UserProfile.tsx
```

#### 1.1.3 接口和类型命名
- 使用 **大驼峰命名法** (PascalCase)
- 接口名以 `I` 开头（可选，但保持一致）
- 类型别名以 `T` 开头（可选，但保持一致）

```typescript
// 接口
interface IUser {
  id: number;
  userName: string;
  email: string;
}

// 类型别名
type TUserRole = 'admin' | 'user' | 'guest';
```

### 1.2 数据结构规范

#### 1.2.1 API 响应字段
- 前端接收后端数据时，直接使用 **驼峰命名法** (camelCase) 访问字段名
- 后端通过全局序列化处理自动将下划线命名转换为驼峰命名

```typescript
// 后端自动转换为驼峰命名返回
{
  userName: '张三',
  createTime: '2024-01-01',
  isActive: true
}

// 前端直接使用
const userName = response.userName;
const createTime = response.createTime;
const isActive = response.isActive;
```

#### 1.2.2 状态管理
- 使用 `useState` 管理组件状态
- 复杂状态使用对象或自定义 Hook 管理
- 避免过度嵌套的状态结构

```typescript
// 好的状态管理
const [user, setUser] = useState<IUser>({
  id: 0,
  userName: '',
  email: ''
});

// 不好的状态管理
const [userId, setUserId] = useState(0);
const [userName, setUserName] = useState('');
const [userEmail, setUserEmail] = useState('');
```

### 1.3 组件规范

#### 1.3.1 通知组件使用
- 使用 `toast` 组件替代 `alert` 进行用户反馈
- 导入 `useToast` Hook 并通过 `toast.success/error/info` 显示不同类型的通知
- 确保在组件外层包裹 `ToastProvider`

```typescript
// 导入
import { useToast } from '../components/common/ToastProvider';

// 使用
const toast = useToast();

// 成功通知
toast.success('操作成功');

// 错误通知
toast.error('操作失败，请重试');

// 信息通知
toast.info('开始批量下载 5 个文件...');

// 示例：删除操作
const handleDelete = async (id: number) => {
  try {
    await deleteResource(id);
    toast.success('删除成功');
    // 更新列表
  } catch (error) {
    toast.error('删除失败，请重试');
  }
};

// 示例：文件大小验证
const handleFileUpload = (file: File) => {
  if (file.size > 5 * 1024 * 1024) {
    toast.error('图片大小不能超过 5MB');
    return;
  }
  // 继续上传
};

// 示例：URL 验证
const handleSubmit = (url: string) => {
  if (!isValidUrl(url)) {
    toast.error('请输入有效的图片URL');
    return;
  }
  // 继续处理
};
```

#### 1.3.2 组件结构
- 每个组件应有清晰的职责
- 组件应保持小巧，避免单个组件过大
- 使用组件化思想，将复杂组件拆分为多个简单组件

```typescript
// 组件结构示例
const UserList: React.FC = () => {
  const [users, setUsers] = useState<IUser[]>([]);
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    fetchUsers();
  }, []);

  const fetchUsers = async () => {
    setIsLoading(true);
    // API 调用
    setIsLoading(false);
  };

  return (
    <div className="user-list">
      {isLoading && <div>加载中...</div>}
      {users.map(user => (
        <UserItem key={user.id} user={user} />
      ))}
    </div>
  );
};
```

#### 1.3.2 Props 类型
- 为组件 Props 定义明确的类型
- 使用 TypeScript 接口确保类型安全

```typescript
interface UserItemProps {
  user: IUser;
  onEdit?: (id: number) => void;
  onDelete?: (id: number) => void;
}

const UserItem: React.FC<UserItemProps> = ({ user, onEdit, onDelete }) => {
  return (
    <div className="user-item">
      <h3>{user.userName}</h3>
      <p>{user.email}</p>
      <button onClick={() => onEdit?.(user.id)}>编辑</button>
      <button onClick={() => onDelete?.(user.id)}>删除</button>
    </div>
  );
};
```

## 2. 后端代码规范

### 2.1 命名规范

#### 2.1.1 变量和函数命名
- 使用 **下划线命名法** (snake_case)
- 变量名应具有描述性，避免使用缩写
- 函数名应清晰描述其功能

```python
# 好的命名
is_loading = True
user_name = '张三'
create_user = lambda name: User.objects.create(name=name)

# 不好的命名
a = True
uname = '张三'
crt_user = lambda n: User.objects.create(name=n)
```

#### 2.1.2 类命名
- 使用 **大驼峰命名法** (PascalCase)
- 类名应清晰描述其功能

```python
# 类名
class UserProfile(models.Model):
    pass

class UserSerializer(serializers.ModelSerializer):
    pass
```

#### 2.1.3 模型字段命名
- 使用 **下划线命名法** (snake_case)
- 字段名应具有描述性

```python
# 模型字段
class User(models.Model):
    user_name = models.CharField(max_length=100)
    email = models.EmailField(unique=True)
    create_time = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

### 2.2 参数校验规范

#### 2.2.1 序列化器使用
- 使用 Django REST Framework 的 `serializers` 进行参数校验
- 为每个模型或接口定义对应的序列化器
- 验证逻辑应写在序列化器中

```python
# 序列化器示例
from rest_framework import serializers
from .models import User

class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['id', 'user_name', 'email', 'create_time', 'is_active']
        read_only_fields = ['id', 'create_time', 'is_active']

    def validate_email(self, value):
        # 邮箱唯一性验证
        if User.objects.filter(email=value).exists():
            raise serializers.ValidationError('该邮箱已存在')
        return value

    def validate_user_name(self, value):
        # 用户名长度验证
        if len(value) < 2:
            raise serializers.ValidationError('用户名长度不能少于2个字符')
        return value
```

#### 2.2.2 视图中使用序列化器
- 在视图中使用序列化器进行参数校验和数据转换
- 处理序列化器验证错误

```python
# 视图中使用序列化器
from rest_framework import viewsets
from .models import User
from .serializers import UserSerializer
from utils.response_utils import ResponseUtils

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserSerializer
    response_utils = ResponseUtils()

    def create(self, request, *args, **kwargs):
        serializer = self.get_serializer(data=request.data)
        if serializer.is_valid():
            serializer.save()
            return self.response_utils.success(data=serializer.data)
        return self.response_utils.error(message='参数验证失败', data=serializer.errors)
```

### 2.3 响应封装规范

#### 2.3.1 使用 response_utils
- 所有 API 响应应使用 `utils.response_utils.ResponseUtils` 进行封装
- 确保响应格式统一
- 后端通过全局序列化处理自动将下划线命名转换为驼峰命名

```python
# response_utils 示例
from rest_framework.response import Response
from utils.error_codes import ErrorCodes

class ResponseUtils:
    def success(self, data=None, message='操作成功'):
        return Response({
            'code': ErrorCodes.SUCCESS,
            'message': message,
            'data': data
        })

    def error(self, code=ErrorCodes.SYSTEM_ERROR, message='操作失败', data=None):
        return Response({
            'code': code,
            'message': message,
            'data': data
        })

    def page(self, queryset, serializer_class, request):
        # 分页响应
        pass
```

#### 2.3.2 响应格式
- 统一响应格式应包含 `code`、`message`、`data` 字段
- `code`: 错误码，`0` 表示成功
- `message`: 响应消息
- `data`: 响应数据（自动转换为驼峰命名）

```json
# 成功响应
{
  "code": 0,
  "message": "操作成功",
  "data": {
    "id": 1,
    "userName": "张三",
    "email": "zhangsan@example.com"
  }
}

# 错误响应
{
  "code": 400,
  "message": "参数验证失败",
  "data": {
    "email": ["该邮箱已存在"]
  }
}
```

### 2.4 错误码管理规范

#### 2.4.1 使用 error_codes
- 所有错误码应定义在 `utils.error_codes` 中
- 错误码应具有清晰的描述

```python
# error_codes 示例
class ErrorCodes:
    # 通用错误码
    SUCCESS = 0
    SYSTEM_ERROR = 10000
    PARAM_ERROR = 10001
    AUTH_ERROR = 10002
    PERMISSION_ERROR = 10003
    NOT_FOUND = 10004
    
    # 业务错误码
    USER_EXISTS = 20000
    USER_NOT_EXISTS = 20001
    PASSWORD_ERROR = 20002
    EMAIL_EXISTS = 20003
```

#### 2.4.2 错误码使用
- 在视图中使用错误码表示不同的错误类型
- 避免直接使用数字错误码，应使用 `ErrorCodes` 类中的常量

```python
# 错误码使用
from utils.response_utils import ResponseUtils
from utils.error_codes import ErrorCodes

response_utils = ResponseUtils()

# 成功响应
return response_utils.success(data=data)

# 参数错误
return response_utils.error(code=ErrorCodes.PARAM_ERROR, message='参数验证失败')

# 用户不存在
return response_utils.error(code=ErrorCodes.USER_NOT_EXISTS, message='用户不存在')
```

### 2.5 视图规范

#### 2.5.1 视图类型选择
- 根据需求选择合适的视图类型：`APIView`、`GenericAPIView`、`ModelViewSet` 等
- 优先使用 DRF 的通用视图和视图集

```python
# APIView
from rest_framework.views import APIView

class UserAPIView(APIView):
    def get(self, request):
        pass

# ModelViewSet
from rest_framework import viewsets

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

#### 2.5.2 权限和认证
- 在视图中明确设置权限和认证类
- 避免在视图中硬编码权限检查

```python
from rest_framework import viewsets, permissions, authentication

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserSerializer
    authentication_classes = [authentication.TokenAuthentication]
    permission_classes = [permissions.IsAuthenticated]

    # 自定义权限
    def get_permissions(self):
        if self.action in ['create', 'list']:
            return [permissions.AllowAny()]
        return [permissions.IsAuthenticated()]
```

## 3. 前后端协作规范

### 3.1 数据格式转换

#### 3.1.1 字段命名转换
- 后端使用 **下划线命名法** (snake_case) 存储数据
- 前端使用 **驼峰命名法** (camelCase) 处理数据
- 后端通过全局序列化处理自动进行字段名转换：
  - 前端发送请求时使用驼峰命名，后端自动转换为下划线命名
  - 后端返回响应时使用下划线命名，自动转换为驼峰命名

```python
# 后端模型 (snake_case)
class User(models.Model):
    user_name = models.CharField(max_length=100)
    create_time = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)

# 后端序列化器 (自动处理转换)
class UserSerializer(serializers.ModelSerializer):
    class Meta:
        model = User
        fields = ['user_name', 'create_time', 'is_active']

# 前端接口定义 (camelCase)
interface IUser {
  userName: string;
  createTime: string;
  isActive: boolean;
}
```

#### 3.1.2 日期时间格式
- 后端返回格式化的日期时间字符串 (YYYY-MM-DD HH:MM:SS)
- 前端根据需要进行格式化

```python
# 后端返回 (自动转换为驼峰命名)
{
  "createTime": "2024-01-01 12:00:00"
}

# 前端处理
const formattedDate = new Date(createTime).toLocaleString();
```

### 3.2 API 接口规范

#### 3.2.1 RESTful API
- 遵循 RESTful API 设计规范
- 使用合适的 HTTP 方法：GET、POST、PUT、PATCH、DELETE

```python
# RESTful API 示例
from rest_framework import viewsets

class UserViewSet(viewsets.ModelViewSet):
    # GET /users/ - 获取用户列表
    # POST /users/ - 创建用户
    # GET /users/{id}/ - 获取用户详情
    # PUT /users/{id}/ - 更新用户
    # DELETE /users/{id}/ - 删除用户
    queryset = User.objects.all()
    serializer_class = UserSerializer
```

#### 3.2.2 API 文档
- 为每个 API 接口编写清晰的文档
- 文档应包含：接口 URL、方法、参数、返回值、错误码等

## 4. 通用规范

### 4.1 代码注释

#### 4.1.1 函数和类注释
- 使用文档字符串（docstring）为函数和类添加注释
- 注释应包含：功能描述、参数、返回值、异常等

```python
# Python 文档字符串
def create_user(user_name, email):
    """
    创建用户
    
    Args:
        user_name (str): 用户名
        email (str): 邮箱
    
    Returns:
        User: 创建的用户对象
    
    Raises:
        ValueError: 邮箱已存在
    """
    if User.objects.filter(email=email).exists():
        raise ValueError('邮箱已存在')
    return User.objects.create(user_name=user_name, email=email)
```

```typescript
// TypeScript JSDoc
/**
 * 创建用户
 * @param {string} userName - 用户名
 * @param {string} email - 邮箱
 * @returns {Promise<User>} 创建的用户对象
 */
const createUser = async (userName: string, email: string): Promise<User> => {
    // 实现
};
```

#### 4.1.2 复杂代码注释
- 对复杂的代码逻辑添加注释
- 注释应解释为什么这样做，而不是怎么做

```python
# 好的注释
# 使用索引而不是列表推导式以提高性能
# 因为数据量可能很大
result = []
for i in range(len(data)):
    if data[i]['status'] == 'active':
        result.append(data[i])

# 不好的注释
# 遍历数据
result = []
for i in range(len(data)):
    if data[i]['status'] == 'active':
        result.append(data[i])
```

### 4.2 代码风格

#### 4.2.1 缩进
- Python 使用 4 个空格进行缩进
- TypeScript 使用 2 个空格进行缩进

#### 4.2.2 换行
- 每行代码长度不应超过 80 个字符
- 长行应适当换行

```python
# 长行换行
users = User.objects.filter(
    is_active=True,
    create_time__gte=datetime.datetime.now() - datetime.timedelta(days=30)
).order_by('-create_time')
```

```typescript
// 长行换行
const users = await fetchUsers(
  { isActive: true, page: 1, pageSize: 10 },
  { timeout: 10000, retry: 3 }
);
```

### 4.3 版本控制

#### 4.3.1 Git 提交规范
- 提交信息应清晰描述提交内容
- 使用英文或中文提交信息，保持一致
- 提交信息格式：`类型: 描述`

```bash
# 提交类型
feat: 添加用户注册功能
fix: 修复登录页面bug
docs: 更新API文档
style: 代码格式调整
refactor: 重构用户模块
test: 添加单元测试
chore: 更新依赖
```

#### 4.3.2 分支管理
- 主分支：`main`
- 开发分支：`develop`
- 功能分支：`feature/功能名称`
- 修复分支：`bugfix/修复名称`
- 发布分支：`release/版本号`

```bash
# 创建分支
git checkout -b feature/user-registration
git checkout -b bugfix/login-error
git checkout -b release/v1.0.0
```

## 5. 总结

代码规范是保证代码质量和可维护性的重要手段。遵循代码规范可以提高开发效率，减少错误，便于团队协作。本规范涵盖了前端和后端的命名规范、代码风格、API 设计等方面，希望团队成员能够严格遵守。

随着项目的发展，代码规范可能会不断更新和完善，团队成员应及时关注并遵守最新的规范。